spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-location: classpath:/configurations

server:
  port: 8888
